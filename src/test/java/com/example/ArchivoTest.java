package com.example;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.fail;
import static org.mockito.Mockito.doNothing;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.mockStatic;
import static org.mockito.Mockito.when;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.MockedStatic;



public class ArchivoTest {

    // @Test
    // public void obtenerDatos(){
    //     UsuarioEsquema usuario = mock(UsuarioEsquema.class);
    //     when(usuario.getNombre()).thenReturn("dany");
    //     Archivo archivo = mock(Archivo.class);
    //     when(archivo.getUser(usuario.getNombre())).thenReturn(usuario);
    //     assertEquals(usuario, archivo.getUser(usuario.getNombre()));
    // }

}




